import { createAction } from 'redux-actions';

export const viewCredits = createAction('appStatus__viewCredits');
export const addCredit = createAction('appStatus__addCredit');
export const editCredit = createAction('appStatus__editCredit');
